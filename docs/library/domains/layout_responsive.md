# 📱 Layout & Responsive (반응형 레이아웃)

모바일 경험(UX)을 최우선으로 고려하는 **Mobile-First** 전략을 따릅니다.

## 📏 Breakpoints
- **Mobile**: Default (`< 640px`)
- **Tablet**: `sm` (`640px`) ~ `md` (`768px`)
- **Desktop**: `lg` (`1024px`) ~

## 🏛️ Decision History

### [2025-12-19] Mobile Typography & Spacing
- **Issue**: PC 기준으로 잡아둔 폰트(`3xl`)가 모바일에서는 작게 느껴지고, 여백은 너무 광활했음.
- **Fix**:
  - **Font Size**: 모바일 `text-4xl`, 태블릿 이상 `text-5xl`로 오히려 모바일 폰트를 키움 (가독성 확보).
  - **Spacing**: 모바일에서는 `gap-6` → `gap-4`, `p-8` → `p-4`로 여백을 타이트하게 줄임.

### [2025-12-20] Player Page Container
- **Issue**: 영상 좌우 여백이 맞지 않아 오버레이가 어색하게 보임.
- **Fix**: `PlayerPage`의 컨테이너 `max-w`와 `padding`을 조정하여 영상 비율(16:9)과 자연스럽게 어우러지도록 함.
- **Style**: 영상과 하단 정보창에 `rounded-3xl`을 적용하여 부드러운 느낌 강조.

## ⚠️ Cautions

1. **Height Calculation (`100vh`)**:
   - 모바일 브라우저 주소창 때문에 `100vh`는 스크롤을 유발합니다.
   - 대신 동적 계산된 높이나 `dvh`(Dynamic Viewport Height) 사용을 고려하십시오.
   - 단, 호환성을 위해 `min-h-screen`을 기본으로 사용 중입니다.

2. **Overflow**:
   - 애니메이션 등으로 인해 X축 스크롤이 생기지 않도록 `overflow-x-hidden`을 루트 컨테이너에 적용하십시오.
