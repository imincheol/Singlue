# 개발 예정 및 개선 목록 (Backlog & Roadmap)

이 문서는 현재 개발되지 않았거나, 임시로 처리된 부분, 그리고 향후 개선이 필요한 사항을 정리합니다.

## 1. 미개발 / To-Do (Not Implemented)
- [ ] **가사 편집 기능**: 생성된 가사나 싱크가 틀렸을 경우 사용자가 수동으로 수정하는 UI.
- [ ] **재생 목록(Playlist) 관리**: 단순 히스토리가 아닌 사용자 정의 플레이리스트 생성.
- [ ] **백엔드 연동**: 현재는 LocalStorage에 의존하므로 기기 간 동기화 불가. Supabase 등을 이용한 클라우드 저장소 연동 고려.

## 2. 임시 처리 / 개선 필요 (Refinement Needed)
- [ ] **Gemini API JSON 파싱 안정성**: `gemini.ts`에서 단순히 `replace`로 마크다운을 제거하고 있는데, 가끔 형식이 깨져서 올 경우 에러가 발생할 수 있음. Zod 등을 이용한 구조 검증 필요.
- [ ] **API ID 생성**: 현재 `crypto.randomUUID()`로 임시 ID를 생성 중. 향후 DB 도입 시 ID 체계 변경 필요.
- [ ] **히스토리 제한**: 현재 하드코딩으로 최대 50개까지만 저장됨. 페이지네이션이나 무한 스크롤 도입 시 로직 변경 필요.
- [ ] **에러 핸들링**: API 호출 실패 시 사용자에게 보여주는 토스트 메시지나 피드백 UI가 단순함 (`alert` 또는 콘솔 로그).

## 3. 데모 데이터 및 테스트 (Testing)
- [ ] **Mock Data**: 현재 데모용 하드코딩 데이터 없음. 처음 사용자는 빈 화면을 보게 됨. 온보딩을 위한 샘플 노래 데이터 추가 고려.
- [ ] **Unit Tests**: 주요 로직(`store`, `gemini.ts`)에 대한 테스트 코드 부재.

## 4. UI/UX 개선
- [ ] **Loading Skeleton**: 데이터 로딩 시 스켈레톤 UI 적용 미흡.
- [ ] **모바일 터치 최적화**: 슬라이더 등의 터치 영역 확장 필요.
